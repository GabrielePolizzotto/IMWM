import { FC } from "react";

export const ViewLogin: FC = (): JSX.Element => {
  return (
    <>
      <input type="text" />
      <input type="password" />
      <button onClick={() => {}}>Entra</button>
    </>
  );
};
